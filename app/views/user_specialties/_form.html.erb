<%= form_with(model: user_specialty, local: true) do |form| %>
  <% if user_specialty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_specialty.errors.count, "error") %> prohibited this user_specialty from being saved:</h2>

      <ul>
        <% user_specialty.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :specialty_id %>
    <%= form.text_field :specialty_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
